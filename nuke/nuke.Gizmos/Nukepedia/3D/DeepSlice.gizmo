Group {
 name DeepSlice
 tile_color 0x59ff
 selected true
 addUserKnob {20 User l DeepSlice}
 addUserKnob {26 "" +STARTLINE}
 addUserKnob {7 nearPlane l "Near Plane" R 0 50}
 nearPlane 6.5
 addUserKnob {7 planeThickness l "Plane Thickness" R 0 50}
 planeThickness 0.4
 addUserKnob {26 "" +STARTLINE}
 addUserKnob {26 name_1 l "" +STARTLINE T "               DeepSlice 1.00     ©  "}
 addUserKnob {22 linkedin -STARTLINE T "import os\nimport sys\n\ntry:\n    import subprocess\n    def _run(cmd, shell, wait):\n        opener = subprocess.Popen(cmd, shell=shell)\n        if wait:\n            opener.wait()\n        return opener.pid\nexcept ImportError:\n    import popen2\n    def _run(cmd, shell, wait):\n        opener = popen2.Popen3(cmd)\n        if wait:\n            opener.wait()\n        return opener.pid\n\ndef _open(url, wait=0):\n    if sys.platform == \"darwin\":\n        cmd = \[\"open\", url]\n    elif hasattr(os, \"startfile\"):\n        return os.startfile(url)\n    elif os.environ.has_key(\"KDE_FULL_SESSION\") or os.environ.has_key(\"KDE_MULTIHEAD\") or \\\n        os.environ.has_key(\"GNOME_DESKTOP_SESSION_ID\") or os.environ.has_key(\"GNOME_KEYRING_SOCKET\"):\n        cmd = \[\"xdg-open\", url]\n    else:\n        raise OSError, \"Desktop not supported.\"\n\n    return _run(cmd, 0, wait)\n\n_open('http://www.linkedin.com/in/lucjulien')"}
}
 Input {
  inputs 0
  name Input1
  selected true
  xpos 493
  ypos -200
 }
set N214265c0 [stack 0]
 DeepExpression {
  chans1 deep
  rgba.red 0
  rgba.green 0
  rgba.blue 0
  rgba.alpha 1
  deep.front "\[value DeepSlice.nearPlane]"
  deep.back deep.front
  name DeepExpression1
  xpos 365
  ypos -119
 }
set N21429d10 [stack 0]
 DeepExpression {
  chans0 deep
  chans1 none
  deep.front "deep.front + \[value DeepSlice.planeThickness]"
  deep.back deep.front
  name DeepExpression2
  xpos 365
  ypos -38
  addUserKnob {20 User}
  addUserKnob {7 Deep_Offset l Offset R -10 10}
  Deep_Offset {{parent.SliceThickness x33 0.72}}
 }
push $N21429d10
push $N214265c0
 DeepMerge {
  inputs 2
  operation holdout
  drop_hidden true
  name DeepMerge3
  xpos 664
  ypos -119
 }
push $N214265c0
 DeepMerge {
  inputs 2
  operation holdout
  drop_hidden true
  name DeepMerge4
  xpos 493
  ypos 11
 }
 DeepMerge {
  inputs 2
  operation holdout
  drop_hidden true
  name DeepMerge5
  xpos 365
  ypos 84
 }
 Output {
  name Output1
  xpos 365
  ypos 186
 }
end_group
